interface:
  display_name: "Trip Sheets"
  short_description: "Repo-specific patterns and workflows for trip-sheets"
  default_prompt: "Use the trip-sheets repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true