# 產物與相依
.next/
out/
coverage/
node_modules/
pnpm-lock.yaml
tsconfig.tsbuildinfo

# 由 API 產生的資料檔(格式由 script 決定,不受 Prettier 管)
data/trip-data.json
data/stores.json

# 手寫文件:規格、SOP、筆記,排版由作者決定,不受 Prettier 管
doc/

# 工具產物
graphify-out/
public/sw.js

# google-github-actions/auth 在 workspace 根目錄寫下的短期憑證設定檔。
# 它是 ADC 的來源(不能不產生),但不是原始碼,也絕不該進版控。
gha-creds-*.json

# Agent harness 設定:非專案原始碼,且部分檔案不是合法 YAML
.agents/
.claude/
.codex/
