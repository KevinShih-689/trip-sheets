name: Deploy to GitHub Pages

on:
  workflow_dispatch: {} # GitHub 手機 App 手動 rebuild(主要更新路徑)
  schedule:
    # 每週日 UTC 21:00 = 台灣週一 05:00 自動兜底。
    # 排程只是兜底,Sheet 改完要立刻上線請用 workflow_dispatch 手動觸發。
    # 頻率壓到每週也讓 fetch-stores 的 API 用量遠離免費額度上限(每月僅 4–5 次)。
    - cron: '0 21 * * 0'
  push:
    branches: [main]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    env:
      BASE_PATH: /${{ github.event.repository.name }}
      NEXT_PUBLIC_GMAPS_EMBED_KEY: ${{ secrets.NEXT_PUBLIC_GMAPS_EMBED_KEY }}
      NEXT_PUBLIC_SHEETS_URL: ${{ secrets.NEXT_PUBLIC_SHEETS_URL }}
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Fetch sheet data
        env:
          GOOGLE_SERVICE_ACCOUNT_KEY: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_KEY }}
          SHEET_ID: ${{ secrets.SHEET_ID }}
        run: pnpm fetch-sheet

      # Places/Geocoding 反查結果的快取:只有新增或修改過的店家才會真的打 API。
      # 快取遺失只會讓這次 build 全量重查(仍在免費額度內),不影響正確性。
      - name: Restore store lookup cache
        uses: actions/cache@v4
        with:
          path: .cache
          key: stores-cache-${{ github.run_id }}
          restore-keys: stores-cache-

      # 預檢:算出這次會打幾次計費 API 並寫進 job summary。超過 MAX_PLACE_CALLS
      # 就在這裡中止 —— 此步驟本身不呼叫任何計費 API,所以中止時費用為零。
      - name: Preflight store lookup usage
        env:
          GOOGLE_SERVICE_ACCOUNT_KEY: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_KEY }}
          SHEET_ID: ${{ secrets.SHEET_ID }}
          MAX_PLACE_CALLS: '400'
        run: pnpm fetch-stores:check

      - name: Fetch store suggestions
        env:
          GOOGLE_SERVICE_ACCOUNT_KEY: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_KEY }}
          SHEET_ID: ${{ secrets.SHEET_ID }}
          GMAPS_SERVER_KEY: ${{ secrets.GMAPS_SERVER_KEY }}
          MAX_PLACE_CALLS: '400'
        run: pnpm fetch-stores

      - name: Format check
        run: pnpm format:check

      - name: Lint & type-check
        run: pnpm lint && pnpm type-check

      - name: Unit tests
        run: pnpm test

      - name: Build
        run: pnpm build

      - name: Sensitive-field guard (spec §3.2 / §8)
        run: |
          if grep -rE "訂位代號|PNR|訂單編號" out/ data/trip-data.json; then
            echo "::error::敏感欄位出現在靜態產物,中止部署"
            exit 1
          fi
          echo "sensitive-field guard passed"

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: out

      - id: deployment
        uses: actions/deploy-pages@v4
